# Copy this file to .env and fill in real values. .env is git-ignored.

# Hugging Face access token — only required if you switch to a gated model.
# The default model (microsoft/Phi-3-medium-4k-instruct) is NOT gated, so this
# can usually be left blank.
HF_TOKEN=your_huggingface_token_here
